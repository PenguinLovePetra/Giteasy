<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Giteasy.Views.Pages.BranchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="Page_Loaded">

    <Grid Padding="0" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="ブランチ管理" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock x:Name="CurrentBranchText" Text=""
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="更新" Click="Refresh_Click" VerticalAlignment="Top"/>
        </Grid>

        <!-- 新規ブランチ作成 -->
        <Grid Grid.Row="1" CornerRadius="8"
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="1" Padding="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewBranchBox"
                     PlaceholderText="新しいブランチ名を入力（例: feature/login）"/>
            <Button Grid.Column="1" Content="作成" Margin="8,0,0,0"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="CreateBranch_Click"/>
        </Grid>

        <!-- ブランチ一覧 -->
        <Grid Grid.Row="2" CornerRadius="8"
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView x:Name="BranchListView" Grid.Row="0" Padding="4"
                      SelectionMode="Single"
                      SelectionChanged="BranchList_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,6" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="{Binding Icon}" FontSize="16"
                                      VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding SyncStatusText}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                           Visibility="{Binding IsSynced}"/>
                            </StackPanel>
                            <Border Grid.Column="3" CornerRadius="4" Padding="8,2"
                                    Visibility="{Binding IsHead}"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}">
                                <TextBlock Text="現在" FontSize="12" Foreground="White"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- 操作ボタン群 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Padding="12,8"
                        Background="{ThemeResource LayerFillColorAltBrush}"
                        CornerRadius="0,0,8,8">
                <Button x:Name="CheckoutBtn" Content="切り替え" Click="Checkout_Click"
                        IsEnabled="False"/>
                <Button x:Name="MergeBtn" Content="マージ" Click="Merge_Click"
                        IsEnabled="False"/>
                <Button x:Name="DeleteBtn" Content="削除" Click="Delete_Click"
                        IsEnabled="False" Foreground="#F44336"/>
            </StackPanel>

            <ProgressRing x:Name="LoadingRing" Grid.Row="0"
                          IsActive="False" Width="40" Height="40"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
